class Timer {
  constructor(  timeLeft ) {
    this.timeLeft = timeLeft
  }

  displayTimeLeft() {
    document.getElementsByClassName('timer-body')[0].innerHTML = "18:00"
    // timeRemaining = '18:00'
    //get the element by its id and update the div
    //first hardcode something in
    //then allow the user to give input to change the time.
    //the above comes later
  }

  //reset timeLeft

  //start timer

  //stop timer

  //pause timer

}
